{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install -g corepack@latest && corepack enable && corepack prepare pnpm@9.15.0 --activate && cd ../.. && corepack pnpm@9.15.0 install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && corepack pnpm@9.15.0 --filter @k3/web build",
  "crons": [
    {
      "path": "/api/queue/scheduled/daily",
      "schedule": "0 6 * * *"
    }
  ]
}
